define:
    int a
    int prime
    int i

program:
    read a

    prime = 1

    i = 2
    while i * i <= a
        if (a / i) * i == a
            prime = 0
        end
        i = i+1
    end

    if prime == 1
        printStr jestpierwsza
    else
        printStr niejestpierwsza
    end